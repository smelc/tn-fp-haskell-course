{
  "name": "tn-fp-haskell-course",
  "image": "ghcr.io/smelc/tn-fp-haskell-course:latest",
  // Refresh the image on every start. Dev Containers resolves ":latest"
  // locally, so without this it never revisits the registry.
  // '|| exit 0' keeps startup working offline.
  "initializeCommand": "docker pull ghcr.io/smelc/tn-fp-haskell-course:latest || exit 0",
  "remoteUser": "vscode",
  "forwardPorts": [8000, 6419],
  "portsAttributes": {
    "8000": { "label": "Slides (python http.server)" },
    "6419": { "label": "grip (exam.md preview)" }
  },
  "postCreateCommand": "cabal build all",
  "customizations": {
    "vscode": {
      "extensions": [
        "haskell.haskell",
        "justusadam.language-haskell"
      ],
      "settings": {
        "haskell.manageHLS": "GHCup",
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
